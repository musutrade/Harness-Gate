# Split Config Module

This change decomposes the workflow configuration implementation by
responsibility while preserving the existing `crate::config` boundary.

- ADR: [0008-config-module-decomposition](../../../docs/adr/0008-config-module-decomposition.md)
- Status: implementation and local verification complete; PR CI pending
