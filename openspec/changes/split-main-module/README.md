# Split CLI Entry Module

This change decomposes CLI definitions, application dispatch, and output
rendering while preserving the existing commands, arguments, and behavior.

- ADR: [0015-main-module-decomposition](../../../docs/adr/0015-main-module-decomposition.md)
- Status: merged to `main`; `main` CI passed
