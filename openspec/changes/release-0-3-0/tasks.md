# Tasks: Release Harness-Gate 0.3.0

- [ ] Update package version, lockfile, CLI version assertion, changelog, and
  documented release links (P0, S).
- [ ] Run package validation, formatting, linting, and the full test suite (P0, M).
- [ ] Create the release PR and verify all required CI checks (P0, M).
- [ ] Merge the release PR and tag `v0.3.0` (P0, S).
- [ ] Verify the GitHub Release assets and crates.io publication (P0, M).
