# Frozen execution steps

Generated by `python3 docs/dogfood/arc-admin/reproduce.py --write`.

Every row blocks when selected. P denotes membership in `policy.required_steps`.

| Step | P | Profiles | Timeout seconds / override | Parser / service | Command (cwd) | Log |
| --- | --- | --- | --- | --- | --- | --- |
| backend.format | yes | full, hook | 180 | — / — | `cargo fmt --manifest-path {backend}/Cargo.toml -- --check` (`{root}`) | `backend_fmt.log` |
| backend.clippy | yes | full, hook | 180 | — / — | `cargo clippy --manifest-path {backend}/Cargo.toml --locked --all-targets --all-features -- -D warnings -D clippy::allow_attributes_without_reason` (`{root}`) | `backend_clippy.log` |
| backend.compile | yes | full | 180 | — / — | `cargo check --manifest-path {backend}/Cargo.toml --locked --all-targets` (`{root}`) | `backend_check.log` |
| backend.tests | yes | full | 600 / RUST_TEST_TIMEOUT | rust / test-postgres | `cargo test --manifest-path {backend}/Cargo.toml --locked -- --nocapture` (`{root}`) | `backend_tests.log` |
| frontend.lint | yes | full, hook | 180 | — / — | `npm exec --offline eslint -- src --max-warnings=0` (`{frontend}`) | `frontend_lint.log` |
| frontend.format | yes | full, hook | 180 | — / — | `npm run format:check` (`{frontend}`) | `frontend_format.log` |
| frontend.tests | yes | full | 180 / ANGULAR_TEST_TIMEOUT | angular / — | `npm test -- --watch=false --runner=vitest` (`{frontend}`) | `frontend_tests.log` |
| frontend.e2e | yes | full | 240 | — / — | `npm run e2e` (`{frontend}`) | `frontend_e2e.log` |
| frontend.fullstack-smoke | yes | full | 420 | — / test-postgres | `npm run e2e:fullstack` (`{frontend}`) | `frontend_fullstack_smoke.log` |
| frontend.build | yes | full | 180 / ANGULAR_BUILD_TIMEOUT | — / — | `npm run build` (`{frontend}`) | `frontend_build.log` |
| workflow.hook-syntax | yes | full, hook | 180 | — / — | `sh -n {root}/codex-audit-pipeline/hooks/pre-commit` (`{root}`) | `git_hook_syntax.log` |
| workflow.project-init-tests | yes | full, hook | 60 | — / — | `node --test {root}/scripts/init-project.test.mjs` (`{root}`) | `project_init_tests.log` |
| workflow.framework-release-config | no | full, hook | 60 | — / — | `node {root}/scripts/check-framework-release.mjs` (`{root}`) | `framework_release_config.log` |
| workflow.framework-upgrade-tests | no | full, hook | 60 | — / — | `node --test {root}/scripts/upgrade-framework.test.mjs` (`{root}`) | `framework_upgrade_tests.log` |
| workflow.template-quality | yes | full, hook | 60 | — / — | `node {root}/scripts/check-templates.mjs` (`{root}`) | `template_quality.log` |
| workflow.template-quality-tests | yes | full, hook | 60 | — / — | `node --test {root}/scripts/check-templates.test.mjs` (`{root}`) | `template_quality_tests.log` |
| workflow.observability-config | yes | full, hook | 60 | — / — | `node {root}/scripts/check-observability.mjs` (`{root}`) | `observability_config.log` |
| workflow.tracing-config | yes | full, hook | 60 | — / — | `node {root}/scripts/check-tracing.mjs` (`{root}`) | `tracing_config.log` |
| workflow.production-deployment-config | yes | full, hook | 60 | — / — | `node {root}/scripts/check-production-deployment.mjs` (`{root}`) | `production_deployment_config.log` |
| workflow.audit-retention-config | yes | full, hook | 60 | — / — | `node {root}/scripts/check-audit-retention.mjs` (`{root}`) | `audit_retention_config.log` |
| workflow.api-generation | yes | full | 180 | — / — | `node {root}/scripts/check-api-generation.mjs` (`{root}`) | `api_generation.log` |
| workflow.supply-chain-config | yes | full, hook | 60 | — / — | `node {root}/scripts/check-supply-chain.mjs` (`{root}`) | `supply_chain_config.log` |
| workflow.format | yes | full, hook | 180 | — / — | `cargo fmt --manifest-path {tool_manifest} -- --check` (`{root}`) | `arc_flow_fmt.log` |
| workflow.clippy | yes | full | 180 | — / — | `cargo clippy --manifest-path {tool_manifest} --locked --all-targets --all-features -- -D warnings -D clippy::allow_attributes_without_reason` (`{root}`) | `arc_flow_clippy.log` |
| workflow.tests | yes | full, hook | 120 / RUST_TEST_TIMEOUT | rust / — | `cargo test --manifest-path {tool_manifest} --locked -- --nocapture` (`{root}`) | `arc_flow_tests.log` |
