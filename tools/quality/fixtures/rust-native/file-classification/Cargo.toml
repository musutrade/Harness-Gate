[package]
name = "native-file-classification"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
tokio = { version = "=1.53.1", features = ["rt"] }

[features]
extra = []
