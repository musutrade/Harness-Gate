[package]
name = "gate-observation-macros"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
proc-macro = true

[dependencies]
gate-observation-generator = { path = "../generator" }
syn = { version = "=2.0.119", features = ["parsing"] }
