# Frozen evidence and source manifests hash the committed bytes. Keep text
# checkouts identical on Windows, macOS and Linux; leave binary artifacts intact.
* text=auto eol=lf
# Raw Actions receipts must keep downloaded bytes, including log whitespace.
docs/dogfood/arc-admin/cost/selfhosted/** -text -whitespace
